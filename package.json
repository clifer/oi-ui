{
  "name": "oi-ui",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "description": "A reference app for comparing baseline and Open Inquiry Constitution-guided model responses across GPT, Grok, and Gemini.",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "check": "node --check server.mjs && node scripts/check-ui.mjs"
  },
  "dependencies": {
    "@google/genai": "latest",
    "dompurify": "latest",
    "dotenv": "latest",
    "marked": "latest",
    "openai": "latest"
  },
  "engines": {
    "node": ">=20"
  }
}
